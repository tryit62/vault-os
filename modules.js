const modules=[['comic','COMIC VAULT','Votre bibliothèque'],['projects','PROJETS','Idées et fabrications'],['notes','NOTES','Mémoire rapide'],['tasks','TÂCHES','À faire'],['inventory','INVENTAIRE','Objets et matériel'],['tools','OUTILS','Utilitaires VAULT']];function renderModules(){moduleGrid.innerHTML=modules.map(m=>`<button class="module" data-module="${m[0]}"><strong>${m[1]}</strong><small>${m[2]}</small></button>`).join('');document.querySelectorAll('.module').forEach(b=>b.addEventListener('click',()=>openModule(b.dataset.module)))}function openModule(key){const m=modules.find(x=>x[0]===key);panelTitle.textContent=m?.[1]||'VAULT';panel.classList.remove('hidden');if(['notes','tasks','projects','inventory'].includes(key)){const data=VaultDB.get(key);panelBody.innerHTML=`<div class="add-row"><input id="moduleInput" placeholder="Ajouter…"><button id="moduleAdd">AJOUTER</button></div><div id="items">${data.map(x=>`<div class="item">${escapeHtml(x.text)}<br><small>${x.date}</small></div>`).join('')||'<p>Aucune donnée.</p>'}</div>`;moduleAdd.addEventListener('click',()=>{if(moduleInput.value.trim()){VaultDB.add(key,moduleInput.value.trim());openModule(key)}})}else if(key==='comic'){panelBody.innerHTML='<p>Comic Vault sera connecté ici dans une prochaine étape.</p>'}else panelBody.innerHTML='<p>Chronomètre, QR, NFC et autres outils arriveront ici.</p>';panel.scrollIntoView({behavior:'smooth'})}function escapeHtml(s){return s.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
